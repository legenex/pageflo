node_modules
.pnpm-store
.next
out
dist
build
*.log
.DS_Store
Thumbs.db

# env
.env
.env.local
.env.production
.env.*.local

# payload
src/payload-types.ts
.payload-cache

# editors
.vscode
.idea

# data
data/
volumes/
*.sqlite
*.db

# local dev artifacts
dev.log
.dev.pid
cookies.txt
.dev-cookies
tsconfig.tsbuildinfo
.git-sha
docker-compose.override.yml

# IDE
*.swp

# Claude Code local config (personal state) — but project-shared slash commands
# and agents under .claude/commands/ and .claude/agents/ ARE checked in.
.claude/*
!.claude/commands/
!.claude/agents/

# Scratch migration sets built by scripts/test-release-ordering.mts.
.migrations-scratch-*/

# Throwaway probe scripts (adversarial reviewers, ad-hoc reproduction). Kept in
# scripts/ so their `@/`-aliased imports resolve; never part of the tree.
scripts/.probe-*
scripts/.probe-*.mts

# Fidelity-harness output (screenshots + pairwise JSON) from
# scripts/quiz-template-fidelity.mts. Regenerated on demand, never committed.
artifacts/
