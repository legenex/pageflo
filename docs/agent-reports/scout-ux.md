# SCOUT 6 — UX + product conformance (Landing Pages / Quizzes template system)

Branches: **ui/model** = `origin/claude/landing-pages-ui-model-fix-aqwwoy` (12 commits, tip 8be4e42), **release** = `origin/claude/legalos-release-work-ea1y9i` (8 commits, tip 96685e0). Merge base = `origin/main` = 3b4748d (main already contains release sessions 1–2, gates 1–13: registry, slots, browse-only Templates tabs, publish lifecycle, LP-embeds-a-flow). Mid-merge repo `/home/user/legalos`: HEAD = release 96685e0, MERGE_HEAD = ui/model 8be4e42 — i.e. **ui/model is being merged INTO release**.

---

## 1. The unmet requirement in 8be4e42 (mandate item 1)

Commit message and `docs/template-model-correction.md:286-334` are explicit and agree:

> **NOT DONE: the twelve ported landing-page templates do not mount the shared Quiz Runtime** (= product requirement **G**). Static quiz-looking HTML in the template acts as the functional quiz.

Measured evidence recorded in the doc (on the ui/model branch):
- `src/components/builder/lp/render.tsx:231-247` — the ported branch returns before `quiz`/`quizCtx` are passed (they reach only the node branch `:282-301`).
- `PortedTemplate.tsx:82` — `PortedTemplateView` accepts **no quiz prop**; no mount token anywhere in `src/`.
- `src/lib/lp-templates/quiz_first.ts` — 12 `<button>`, 0 `<form>`, 0 `<input>`.
- Worse than dead buttons: public `page.tsx` sets `hasForm={Boolean(resolved.quiz)}` so **TrustedForm loads on a page that captures nothing**; and ui/model's own `lp-deployment.ts:278-291` validates the bound flow, refuses to serve without it — then never draws it.
- Why not half-fixed: the quiz must replace a region *inside* a section; 2 of 12 templates (`split_screen_direct`, `insurer_vs_claimant`) don't name their quiz region, so the boundary must come from the extractor + re-verification of all twelve.

**What remains to be done: nothing new — the RELEASE branch already built exactly this** (Gate A, commit `364d6e5`): `src/lib/lp-slots/quiz-mount.ts` derives the card region from the reference's own structure (answer group + step indicator/nav ancestor; exactly one region or the extractor refuses), all 12 regenerated `src/lib/lp-templates/*.ts` carry `quizMount`, `PortedTemplate.tsx:112-157` portals the real `QuizRuntime` into the marked box (`useLayoutEffect`, `:210` renders `composed.htmlWithMount` — static card cut out), `render.tsx:244-250` passes `quiz`/`quizCtx` into the ported branch, and `pnpm test:e2e` proved one lead POST from the LP path in a real browser. ui/model never touched `render.tsx`/`PortedTemplate.tsx`/`lp-templates/*` (diff vs main is empty), so **release's side merges clean and G closes mechanically in the integration** — with one residual (see Risks R1).

## 2. What release considers done / undone (mandate item 2)

`docs/template-system-implementation-state.md` (3 sessions; sessions 1–2 already on main):
- **Done** (session 3, on the branch): Gate A LP quiz = real runtime; Gate B `quiz_deployment_id` demoted to migration fallback + `src/lib/lp-quiz-binding.ts` + `reconcile:lp-quiz`; Gate C `[LOGO SLOT]`/`[LEGAL DISCLOSURE]`/`[CASE RESULT]` wells fixed (`liveDefault`, `mustSupply`); Gate D quiz webhook/tier execution server-side (`/api/legalos/quiz-webhook`, 133 asserts); Gate E `/_next/image` SSRF closed; Gates F/G `test:bootstrap` (54) + `scripts/release.sh` + `test:release` (26); Gate H observability reporter; Gate I `test:e2e` (34, production build, one lead per funnel); Gate J adversarial (mount-coordinate refusal, unpublished-flow → 404, reconcile NaN). Totals: typecheck 0, build 0, `pnpm test` 2458/10 suites, dom 357, isolation 12.
- **Undone by release's own account**: EB-1 MVA tier service does not exist (tiers 1/2/4 dead; endpoint answers 405); EB-2 Namecheap/DNS down → **no tenant host serves valid HTTPS — disqualifies production release on its own** (`production-readiness.md`: criterion 1 FAIL, Gate 14 NOT PASS); EB-3 error-tracking destination undecided; advertorials remain a 4th template id space outside the registry.
- `production-readiness.md` corrected statuses: criterion 2 (fresh DB) PASS, 3 (lead delivered) PASS at app layer incl. LP path, 5 (release process) PASS, 6 PARTIAL, 1 FAIL.
- **Release has NO knowledge of the IA/records requirements A–F**: its docs never mention two-tab Landing Pages, template records, CRUD, or 3-tab deployment editors. Gate 7 (on main) *added* the browse-only Templates tab and `EXPECTED_LP_TEMPLATE_COUNT` — the very thing ui/model removes.

## 3. Per-requirement status (mandate item 3)

Legend: file:line refs are on the named worktree. "merged-todo" = what the integrated branch still needs beyond taking the right side.

| Req | ui/model branch | release branch | merged product must still do |
|---|---|---|---|
| **A** LP tabs exactly `Templates \| Deployments`, no Pages, templates ARE records (12 stock materialized) | **MET.** `lp/LandingPagesApp.tsx:1174-1175` exactly 2 tabs, default `templates` (`:870`); records from `funnel-landing-pages` via `src/lib/template-records/index.ts` (stock reconcile, `stock_key`, idempotent; sample rows `MVA Pain First`/`Authority Build`/`Editorial Test` retired by identity-preserving repoint — doc §Sample-record reconciliation); browse-only `TemplateLibrary.tsx` deleted | **NOT MET.** `lp/LandingPagesApp.tsx:1107-1109` = `Pages \| Templates \| Deployments`; Templates count is registry constant `EXPECTED_LP_TEMPLATE_COUNT`; `TemplateLibrary` mounted with no `onUse` (`:1124`) = browse-only stock gallery — the forbidden concept | Resolve UU `LandingPagesApp.tsx` to ui/model's version; drop release's `TemplateLibrary` imports (file is staged `D` in merge — any surviving import breaks the build) |
| **B** Quiz tabs exactly `Quiz Flows \| Templates \| Deployments`, 20 stock quiz-template records | **MET.** `quiz/QuizBuilderApp.tsx:127-129` exact labels/order; new collection `src/collections/FunnelQuizTemplates.ts`; 20 stock seeded `template_id === renderer_key === 'sq_*'` (doc §funnel-quiz-templates); panel `QuizTemplatesPanel.tsx` | **NOT MET.** `QuizBuilderApp.tsx:124-126` = `Quiz Builder \| Deployments \| Templates` (wrong label, wrong order, browse-only `TemplateLibrary` at `:1150`); no records collection | `QuizBuilderApp.tsx` auto-merged to ui/model side (only ui/model changed it since base) — verify post-merge compile; register migration (see R4) |
| **C** Template CRUD everywhere (Preview/Edit/Clone/Enable/Disable/safe Delete/Create blank/Create with Claude) | **MET for LP; quiz side lacks "Create with Claude".** LP list `lp/TemplateListView.tsx:164-183` (Preview/Edit/Enable-Disable/Clone/Delete w/ aria-labels); quiz list `QuizTemplatesPanel.tsx:491-502` + EditorModal `:148`, CreateModal `:281-332` (**blank-from-renderer only**); LP create: "New template with Claude" `LandingPagesApp.tsx:1164` (AI wizard over `SKELETON_TEMPLATES`, origin `ai`) + "New blank template" `:1169`/`:971`; server layer `(top)/template-actions.ts:81-535` — all 10 actions (create/clone/save/setEnabled/delete × lp/quiz); safe delete: referenced or stock → archive (`archived_at`), warning text asserted in test; quiz `origin` enum = stock/clone/blank (no `ai`) — a quiz template is a renderer identity, so Claude-create is arguably not meaningful there, but it IS a literal gap vs the requirement text | **NOT MET.** No record CRUD of any kind; templates are module exports ("you cannot disable, clone or delete a module export") | Decide: accept blank-only quiz-template creation (recommend: yes, renderers are code; document it) or add an AI path; nothing else |
| **D** Quiz deployment tabs exactly `General \| Destination URL's \| Tracking & Pixels`; Render & Embed / Header / Footer / Body Sections REMOVED; General = Name, Quiz Flow, Brand, Domain, Path, Status, Embed-or-Standalone, visual quiz-template gallery (real preview, Preview btn, Select btn, obvious selected state) | **MET.** Tabs `QuizBuilderApp.tsx:464-468`; removal rationale in comment `:456-463`; General: name `:500`, quiz flow `:508-515` (archived-reference warning), brand + BrandQuickEdit `:517-526`, domain (cert-gated options) `:528-546`, path `:547`, status `:549`, render-mode cards + inline embed code `:552-594`, gallery `:610-618` (`TemplateGallery kind="quiz"`, records, selectedId=`templateId`), per-brand contrast audit `:623-630`, progress override `:632-652`. Chrome columns deprecated+hidden; Brand Identity owns chrome (visible behavior change, reported) | **NOT MET.** `QuizBuilderApp.tsx:422-427` = `Basics \| Render & Embed \| Destinations \| Header / Footer \| Body Sections \| Tracking & Pixels`; template choice is a grid inside Render & Embed | Take ui/model (auto-merged); nothing else |
| **E** LP deployment tabs same three; General = Name, **Quiz Flow direct** (NOT standalone quiz deployment), Brand, Domain, Path, Status, visual LP-template gallery, prominent headings | **MET.** New `lp/LPDeploymentEditor.tsx`: tabs `:235-239`; name `:304`, Quiz Flow direct `:311-318` with legacy-pointer migration note `:319-323`, brand `:326-345`, domain `:349-366`, path `:368-370`, status `:374-380`, LP gallery `:403-411`, embedded-quiz-skin override w/ template recommendation `:414-441`; deliberately no render-mode (`:22-24`); `SectionHeading` eyebrow/title/hint everywhere = prominent headings | **PARTIAL.** Editor is still the untabbed single card (`LandingPagesApp.tsx:675`), picks a landing-PAGE row from a dropdown — but Gate A DID add direct flow binding (`draft.quizId`; legacy `quizDeploymentId` display-only `:808`) and fixed the save that wiped `quiz` | Take ui/model editor; port nothing (ui/model already binds `quizId` and preserves the legacy pointer). Verify auto-merged `(top)/landing-pages/actions.ts` `saveDeployment` still writes `quiz` + clears legacy on update (release Gate B semantics; ui/model `actions.ts:91-219` maps `landingPageId → landing_page` via `resolveLpTemplateSelection`) |
| **F** Save/reload preserves selected template record ID; public render uses the same ID; no silent fallback | **MET.** UI: gallery `data-selected` `:254`, SELECTED pill `:289`; persistence proven by browser test (`test-admin-ui.mts:409-437` LP, `:516-533` quiz — select 2nd/3rd card, Save, reload, re-open, still selected). Server: `lp-deployment.ts:193-205` strict (`resolveTemplate`, refuse + `LP_TEMPLATE_REFUSED` log, `templateFellBack` hardcoded false `:32`); `quiz-deployment.ts:102,273-284` resolves the RECORD, no fallback; unknown id → 404 + red badge (`TemplateGallery.tsx:399-422` missing-selection banner distinguishing legacy aliases; deployment list `LandingPagesApp.tsx:140` "Template missing, pick one to fix"); publish preflight requires record exists+enabled (`publish-lifecycle.ts` record gates) | **PARTIAL.** IDs preserved on save, but public render still falls back with a flag: `lp-deployment.ts:273` / `quiz-deployment.ts:221` `templateFellBack: templateRes.usedFallback` — visitor can get the stand-in template (logged, not silent in logs, but silent to the operator on the page) | Merged `lp-deployment.ts` (auto-merged, 425 lines) already keeps ui/model strictness (`:199-205`, `:339 templateFellBack:false`) — **hand-verify** `quiz-deployment.ts` and the UU `test-publish.mts` resolution keep refusal semantics; run `test:identity` + `test:ui` post-merge |
| **G** LP quiz = real shared QuizRuntime at semantic mount; static answers removed/inert | **NOT MET** (the 8be4e42 admission — see §1) | **MET.** `quiz-mount.ts` (region derived, refuse on 0 or 2 matches); `PortedTemplate.tsx:133-157` `QuizMount` portal, `:210` `htmlWithMount` (static card physically cut); `render.tsx:244-250`; Gate J: bound-flow-unpublished → 404; e2e: one lead from LP path | Nothing structural — release side merges clean and ui/model's public `page.tsx:764/:809` passes `quiz={resolved.quiz...}` into `LivePreview`. **But see R1: admin galleries now render an empty hole** |
| **H** Mobile+desktop usable, keyboard accessible, visible focus, no console/hydration errors, no horizontal overflow | **PARTIAL.** Overlays got `role`/`aria-modal`/`aria-label` (ConfirmDialog, Modal, LP preview — doc §browser findings; `TemplateGallery.tsx:160-163`); icon buttons have aria-labels (asserted via `getByRole`); tab buttons expose `aria-current` (`LPDeploymentEditor.tsx:285`, `QuizBuilderApp.tsx:489`); tab rows `overflowX:auto`; gallery grid `minmax(min(100%,320px),1fr)`; PreviewModal has Desktop/Mobile toggle (`:176-177`); suite asserts zero `pageerror` (`test-admin-ui.mts:165-166,330,563`) | **PARTIAL, different half.** Public-side contrast proven in Chromium (`test:dom` 357; brand cannot reduce readability below reference); e2e walks funnels; no admin-a11y work | See §5 — neither branch asserts mobile viewport, keyboard/Tab order, focus visibility, hydration warnings, or overflow-x. This is the one requirement **neither branch fully implements or tests** |

## 4. `scripts/test-admin-ui.mts` — what the browser test actually asserts (mandate item 3b)

722 lines, ~85 assertions, on ui/model only. Requires a running app on `:3000` (`LEGALOS_UI_BASE`) and a super-admin login (`SUPER_ADMIN_EMAIL`/`PASSWORD`, defaults `team@legenex.com` / `local-dev-password-9c1f`); pinned-Chromium fallback path `:703-707`.

**Asserts** (by `data-*` hooks; tab labels deliberately by text `:17-21`): 2 LP tabs / no Pages (`:179-182`); ≥12 LP template rows, sample names absent, real names present (`:186-194`); CRUD verbs by **accessible name** incl. icon buttons (`:210-225`); full clone→verify(origin=clone)→delete round-trip diffing row-ID sets (`:236-280`); stock delete warns "archive" + cancel keeps it (`:287-300`); Disable survives reload (`:302-315`); Preview opens dialog (`:319-325`); LP deployment editor 3 tabs, forbidden tabs GONE (`:347-354`); Quiz Flow selector present and **no standalone quiz-deployment picker** (`:356-370`); Brand/Domain/Path/Status (`:371-374`); visual gallery ≥12 cards, Preview+Select per card (`:376-385`); **thumbnail paints — pixel-samples >8 distinct colors from a real screenshot** (`:398-405`, the anti-iframe-blank check); select→`data-selected`→Save→reload→reopen→persists (`:409-437`); section headings on other tabs (`:441-446`); quizzes 3 tabs (`:453-457`); ≥20 quiz template rows + CRUD (`:462-477`); quiz editor 3 tabs, Embed/Standalone on General, gallery ≥20, select-persist (`:492-533`); negative case: no missing-template banner on a healthy deployment (`:550-561`); zero `pageerror` overall (`:563`); **public-render identity proof** via `node:http` with a real `Host:` header (fetch silently drops Host — `:25-34`): two live deployments of one brand on different templates must 200, differ, each carry its OWN headline slot text and not the other's, cross-SITE path 404s (`:600-673`); **a skip of that block is a FAILURE** since 8be4e42 (`:679-687`).

**Fixture setup: there is none.** The suite creates no data. It relies on (a) server-side stock reconcile materializing 12+20 records when the libraries load, and (b) **pre-existing live deployments** — one brand with two live LP deployments on different templates — for the public proof. **On zero templates it fails loudly, not silently**: `:187`/`:463` fail on count, then the clone flow times out on an empty locator and the whole run lands in the catch at `:714-716` ("FAIL suite threw"), exit 1. On a fresh DB with no live deployments, everything passes except `:687`, which now fails by design. The merged CI story must either seed two live deployments or accept that `test:ui` needs the branch's scratch data.

**Screenshots**: all four exist in `/home/user/wt-uimodel/docs/screenshots/` (`landing-page-templates.png`, `landing-page-deployment-general.png`, `quiz-templates.png`, `quiz-deployment-general.png`), regenerated by the suite (`:196,:407,:479,:514`) and updated in 8be4e42.

## 5. Accessibility / mobile — suite coverage vs requirement H (mandate item 5)

Covered by ui/model: accessible names for every CRUD control (getByRole would fail an unlabeled icon button); dialog roles/labels; `aria-current` on tabs; Escape closes preview (`:326`); zero uncaught client exceptions; real-pixel paint checks; responsive grid + `overflowX:auto` tab bars in code.

**Gaps (merged-todo, nobody owns these yet):**
1. **No mobile viewport pass** — the suite runs only at 1440×1000 (`:155`); the PreviewModal's Mobile toggle is never driven; no admin screen is ever rendered at 390px.
2. **No keyboard-navigation or focus-visibility assertions** — no Tab-order walk, no `:focus-visible` check; requirement H names both.
3. **No horizontal-overflow assertion** (`document.scrollingElement.scrollWidth <= innerWidth`) on any screen.
4. **Hydration errors are not caught**: the suite listens to `pageerror` only; React hydration mismatches log `console.error` without throwing, so "no hydration errors" is unverified on both branches. Add a `page.on('console')` filter for `hydrat`/`did not match`.
5. Release's `test:dom`/`test:e2e` cover public rendering/contrast, not admin UX; no overlap with 1–4.

## 6. Leftover nonconforming UI (mandate item 4)

- **release**: everything in §3 rows A/B/D/E — `Pages` tab (`lp/LandingPagesApp.tsx:1107`), `Basics/Render & Embed/Header / Footer/Body Sections` (`quiz/QuizBuilderApp.tsx:422-427`), browse-only `TemplateLibrary` (both apps), landing-page-row dropdown in the LP editor. All die when the UU files resolve to ui/model.
- **ui/model**: clean in live UI. `grep` for the forbidden labels finds only: `src/lib/buildlog.ts:302,358,1414,2017` — **stale evidence steps** ("Deployments › Edit › Render & Embed…") in the `/admin/buildlog` historical log; same lines exist on release. Not a tab, but the buildlog's capture job and any reader following those steps now hit a tab that no longer exists. Low priority; annotate or mark superseded.
- `BrandModule.tsx:703` "Default Body Sections" is **conforming** — brand-level chrome is exactly where the requirement moved it.
- Advertorial builder (`AdvertorialBuilderApp.tsx:164-165`, tabs `Advertorials | Deployments`) has none of the forbidden tabs; out of A–H scope; still a 4th template space outside the registry (release's own not-done).
- Dead controls / static quiz markup: ui/model galleries render static card pictures inert (`aria-hidden`, `pointerEvents:'none'` in `LpThumb`); on the **public** page ui/model still serves the static card as the quiz (the G violation) — resolved by taking release's side.

## 7. Risks

- **R1 — merge-induced gallery hole (new, found by this scout).** Release's `PortedTemplateView` renders `composed.htmlWithMount` **unconditionally** (`PortedTemplate.tsx:210`) and `QuizMount` returns null without a `quiz` (`:140`). ui/model's `TemplateGallery` mounts `LivePreview` with **no quiz prop** (`TemplateGallery.tsx:124-129,215-220`). Post-merge, every LP template card/preview in the galleries and the template-list previews will draw the template **with an empty cut-out where the quiz card was** — the "real preview" of requirement D/E degrades, and the four committed screenshots no longer match the product. Release's own builder preview solves this by passing a `previewQuiz` (`release LandingPagesApp.tsx:574`). Fix: pass a sample/preview quiz (preview mode) into gallery + preview-modal mounts, or teach `PortedTemplateView` to render `composed.html` when `quiz == null` in admin contexts (public must keep `htmlWithMount`).
- **R2 — auto-merged semantic files.** Both branches edited, git merged without conflict — each needs a human read: `src/lib/lp-deployment.ts` (inspected: coherent — ui/model strictness kept, release type field present, 425 lines), `src/lib/publish-lifecycle.ts` (inspected: both `validateQuizFlow:45` and record gates `:188-231` present), `src/components/public/quiz/QuizRuntime.tsx` (`progressForm` read at `:107` — ui/model's fix intact), `src/lib/funnel-samples.ts` (ui/model's sample retirement × release's Gate B flow-binding starter — highest residual risk of the auto-merges), `quiz/preview.tsx`, `payload.config.ts`, `(top)/landing-pages/actions.ts`.
- **R3 — six live conflicts (UU)**: `package.json` (script sets: ui/model adds `test:ui`/`test:identity`/`test:records`; release adds `test:e2e`/`test:bootstrap`/`test:release`/`test:webhook` — union them), `scripts/test-publish.mts` (union of record gates + flow gates; keep both suites' assertions), `(top)/landing-pages/content-actions.ts` (ui/model template-level `slot_overrides` callers × release Gate C `liveDefault`/`mustSupply`), `(top)/landing-pages/page.tsx` (load template records + quizTemplates AND release's binding verdicts), `lp/LandingPagesApp.tsx` (take ui/model structure; release's unique value — flow-binding pills/`lp-quiz-binding` verdicts in the list — is already covered by ui/model's own missing/legacy pills; verify nothing else release-only is lost), `src/migrations/index.ts` (register all three `20260813_2*` migrations).
- **R4 — migration rename.** ui/model's `20260813_210000_template_records.ts` collides by timestamp with release's `20260813_210000_locked_documents_funnel_rels.ts`; the merge already renamed it `20260813_220000_template_records.ts` (staged `A`) so it sorts after `213000`. Payload reads the DIRECTORY (release corrected CLAUDE.md on this), so the rename is the load-bearing part; `index.ts` registration is convention. Confirm the rename is also reflected anywhere the doc/tests name the migration.
- **R5 — F on the public side.** If any resolver path re-acquires `resolveForRender` fallback during conflict resolution, requirement F silently regresses. `test:identity` (33 DB assertions, ui/model) is the guard — run it post-merge.
- **R6 — screenshots + docs staleness.** After R1 is fixed, re-run `pnpm test:ui` to regenerate the four PNGs; update `docs/template-model-correction.md` "NOT DONE" section (it becomes DONE via merge) or the docs will claim a defect the merged product doesn't have.
- **R7 — environment.** `test:ui` needs a live server + seeded live deployments; release's cloud env is the only one proven to build. EB-1/EB-2/EB-3 are untouched by the merge and still gate production (EB-2 alone disqualifies).

## 8. Ownership

- IA/tabs/galleries/CRUD/records (A–F): ui/model side owns; conflicts resolve toward ui/model in the six UU files.
- Quiz-mount, LP flow binding fallback, tier webhook, e2e/bootstrap/release harnesses (G + infra): release side owns; merges clean.
- New joint work nobody owns yet: R1 preview-quiz wiring; H gaps (§5); quiz-template "Create with Claude" decision (C); buildlog step annotations.

## MERGE GUIDANCE — ordered UI work items for the integrated branch

1. Resolve the six UU files toward ui/model's IA (2-tab LP, 3-tab editors, records-backed galleries); drop every `TemplateLibrary` import (file is deleted in the merge); union `package.json` scripts and `test-publish.mts` assertions; register `20260813_210000_locked_documents_funnel_rels` + `20260813_213000_integration_config_sample_markers` + `20260813_220000_template_records` in `migrations/index.ts`.
2. Fix R1: pass a preview/sample quiz into `TemplateGallery`'s `LivePreview` mounts (thumb + PreviewModal) or render un-mounted `composed.html` when `quiz == null` in admin contexts — public path must keep `htmlWithMount` + portal.
3. Hand-review the auto-merged semantic files (R2), especially `funnel-samples.ts` (sample retirement × starter-flow binding) and `(top)/landing-pages/actions.ts` (`saveDeployment` must keep: record-id mapping, `quiz` write, legacy `quiz_deployment_id` clear-on-update).
4. Re-run the full gauntlet on a machine that can build: `typecheck`, `pnpm test` (union of both branches' suites), `test:identity`, `test:dom`, `test:e2e`, then `pnpm test:ui` — which also regenerates the four screenshots (R6). `test:ui` requires seeding one brand with two live LP deployments on different templates or its final assertion fails by design.
5. Close requirement H's untested half: add a 390px-viewport pass, a Tab-order + `:focus-visible` walk, an `overflow-x` assertion per screen, and a `console`-listener for hydration warnings to `test-admin-ui.mts`.
6. Decide C's quiz-side "Create with Claude" (recommend: document blank-only as the design — quiz templates are renderer identities — or add an AI naming/blurb assist).
7. Housekeeping: mark the `buildlog.ts` entries whose evidence steps reference removed tabs as superseded; update `docs/template-model-correction.md` NOT-DONE section to record that Gate A closed G in the merge.
8. Unchanged external gates before any production claim: EB-2 (HTTPS — disqualifying), EB-1 (tier service), EB-3 (error destination).
