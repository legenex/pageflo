# Wave 01 prompt

Objective: establish three independent foundations.

Lane A W10 owns redesign shell/navigation. Lane B W11 owns Brand/Site identity and hosted legal pages. Lane C W12 owns Lead durability/queue only. Do not let lanes write each other's files. Declare exact paths after current audit before spawning parallel work.

Each lane must run targeted tests and evaluator. Integrator resolves shared files only after lanes hand off. No fake redesign data. No tenant shortcut. Queue work must prove durable persistence and idempotency before replacing working synchronous behavior.

When all three predicates pass, integrate, run fast gate, update state, commit/push/release coherent ordinary changes as appropriate, then continue to Wave 02.
